name: CI/CD Tienda Perritos - Backend - Ventas

on:
  push:
    branches: [ "main" ]
    paths:
      - "back-Ventas_SpringBoot/**"

jobs:
  build-push-deploy-backend:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar credenciales AWS
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login en Amazon ECR (runner)
      uses: aws-actions/amazon-ecr-login@v1
      with:
        mask-password: true

    - name: Build imagen Backend y push a ECR
      run: |
        cd back-Ventas_SpringBoot\Springboot-API-REST
        docker build -t tienda-backend-v .
        docker tag tienda-backend-v:latest ${{ secrets.ECR_REPO_URL_BACKEND_V }}:latest
        docker push ${{ secrets.ECR_REPO_URL_BACKEND_V }}:latest

    - name: Desplegar Backend vía SSM
      run: |
        aws ssm send-command \
          --instance-ids ${{ secrets.EC2_BACKEND_V_INSTANCE_ID }} \
          --document-name "AWS-RunShellScript" \
          --comment "Actualizar tienda-backend-v desde ECR" \
          --parameters '{"commands":[
            "set -e",
            "aws ecr get-login-password --region '${{ secrets.AWS_REGION }}' | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}",
            "docker pull ${{ secrets.ECR_REPO_URL_BACKEND_V }}:latest",
            "docker stop tienda-backend-v || true",
            "docker rm tienda-backend-v || true",
            "docker run -d --name tienda-backend-v -p 3001:3001 ${{ secrets.ECR_REPO_URL_BACKEND_V }}:latest"
          ]}' \
          --output text